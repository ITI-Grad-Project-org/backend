apiVersion: v1
kind: Secret
metadata:
  name: coachhub-secrets
  namespace: coachhub
type: Opaque
stringData:
  # TODO: replace placeholder values before deploying — use a secrets manager in production
  DB_USERNAME: "changeme"
  DB_PASSWORD: "changeme"
  MONGO_USERNAME: "changeme"
  MONGO_PASSWORD: "changeme"
  RABBITMQ_USERNAME: "changeme"
  RABBITMQ_PASSWORD: "changeme"
  JWT_SECRET: "changeme"
  GEMINI_API_KEY: "changeme"
