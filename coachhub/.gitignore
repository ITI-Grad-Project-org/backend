# Node / NestJS
node_modules/
dist/
.env
.env.developement
.env.production
.env.*
!.env.example
*.local
# K8s secret files with real values (the committed one is only a template)
*.local.yaml

# Shell scripts that carry real credentials. A key was once committed as the
# default in `${GEMINI_API_KEY:...}` and Google's scanner disabled it, so
# anything that holds a live value stays out of the repo by name.
secret.sh
secrets.sh
*-secrets.local.sh
set-secrets.sh
# personal notes stay ignored; docs that the code and other docs link to are
# part of the repo and are allowlisted individually
docs/*
!docs/deployment/
!docs/analytics/
!docs/ai/

# Java / Maven
application-local.yml
application-local.yaml
target/
*.class
*.jar
*.war
.mvn/
.settings/
.classpath
.project

# IDE
.idea/
*.iml
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
.editorconfig

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Docker
.docker/

# Prisma
prisma/*.db
prisma/*.db-journal

mydocs
AGENTS.md

.idea
presentation